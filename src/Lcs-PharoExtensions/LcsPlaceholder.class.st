"
This was created to overcome a bug - an extension cannot be put into a package which has no class in it
"
Class {
	#name : #LcsPlaceholder,
	#superclass : #LcsObject,
	#category : #'Lcs-PharoExtensions'
}
